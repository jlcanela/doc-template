{
  "name": "docgen",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "doc": "rm -rf out && mkdir out && cd doc && for file in *.puml; do puml generate $file -o ../out/$file.png; done && cd ..",
    "docM3":"for %i in doc/*.puml   -o out/architecture.png",
    "doc2": "puml generate --unicode --text \"A -> B: Hello\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-plantuml": "^0.9.0"
  }
}
